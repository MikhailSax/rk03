{% extends 'base.html.twig' %}

{% block title %}Временная ошибка сервера{% endblock %}

{% block body %}
    <section class="relative overflow-hidden bg-gradient-to-b from-amber-50 via-white to-white py-16 sm:py-20">
        <div class="pointer-events-none absolute -top-24 left-1/2 h-72 w-72 -translate-x-1/2 rounded-full bg-amber-100 blur-3xl"></div>

        <div class="relative mx-auto max-w-3xl px-6 text-center">
            <span class="inline-flex rounded-full border border-amber-200 bg-white px-3 py-1 text-xs font-semibold uppercase tracking-wide text-amber-700">Ошибка 500</span>
            <h1 class="mt-5 text-4xl font-bold text-gray-900 sm:text-5xl">Сервис временно недоступен</h1>
            <p class="mx-auto mt-4 max-w-2xl text-base text-gray-600 sm:text-lg">
                На сервере произошла непредвиденная ошибка. Мы уже получили сигнал и занимаемся восстановлением.
                Попробуйте обновить страницу через несколько минут.
            </p>

            <div class="mt-9 flex flex-wrap items-center justify-center gap-3">
                <a href="{{ path('app_homepage') }}" class="inline-flex items-center rounded-lg bg-gray-900 px-5 py-3 text-sm font-semibold text-white transition hover:bg-gray-700">
                    Вернуться на главную
                </a>
                <button type="button" onclick="window.location.reload()" class="inline-flex items-center rounded-lg border border-gray-300 bg-white px-5 py-3 text-sm font-semibold text-gray-700 transition hover:border-amber-300 hover:text-amber-700">
                    Обновить страницу
                </button>
            </div>
        </div>
    </section>
{% endblock %}
