<main class="grid min-h-[70vh] place-items-center bg-white px-6 py-16">
    <div class="max-w-lg text-center">
        <p class="text-sm font-semibold uppercase tracking-wide text-red-600">Ошибка 404</p>
        <h1 class="mt-4 text-4xl font-bold text-gray-900 sm:text-5xl">Страница не найдена</h1>
        <p class="mt-4 text-base text-gray-600">
            Возможно, ссылка устарела или страница была перемещена. Перейдите на главную,
            чтобы продолжить работу с каталогом рекламных конструкций.
        </p>
        <div class="mt-8 flex items-center justify-center gap-3">
            <a href="{{ path('app_homepage') }}" class="rounded-lg bg-red-600 px-5 py-3 text-sm font-semibold text-white hover:bg-red-700">
                На главную
            </a>
            <a href="{{ path('app_map_app') }}" class="rounded-lg border border-gray-300 px-5 py-3 text-sm font-semibold text-gray-700 hover:border-red-300 hover:text-red-600">
                Открыть карту
            </a>
        </div>
    </div>
</main>
