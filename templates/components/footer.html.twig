{# =====================================================================
   Подвал (Фаза 1). Скруглённый тёмный блок, в духе v2.
   ===================================================================== #}
<footer class="relative z-10 mx-3 mb-3 mt-auto rounded-[32px] bg-[var(--color-ink)] px-6 py-12 text-[#9aa0b2] sm:mx-4 sm:mb-4 sm:px-10 sm:py-14">
    <div class="mx-auto max-w-[1240px]">
        <div class="grid gap-10 md:grid-cols-2 lg:grid-cols-[2fr_1fr_1.4fr]">
            <div class="lg:max-w-sm">
                <h4 class="font-[var(--font-display)] text-3xl font-extrabold text-white">РК03</h4>
                <p class="mt-4 max-w-[32ch] text-[.96rem] leading-relaxed">
                    Размещаем наружную рекламу в городе и регионе: от подбора конструкций до отчётности по кампании.
                </p>
                <a href="{{ path('app_map_app') }}" class="u-btn u-btn--blue mt-6">Карта конструкций <span class="arr">→</span></a>
            </div>

            <nav aria-label="Навигация в подвале">
                <h5 class="mb-4 text-[.78rem] font-bold uppercase tracking-[0.08em] text-white">Навигация</h5>
                <ul class="space-y-3 text-[.95rem]">
                    <li><a href="{{ path('app_homepage') }}" class="transition hover:text-white">Главная</a></li>
                    <li><a href="{{ path('app_map_app') }}" class="transition hover:text-white">Карта конструкций</a></li>
                    <li><a href="{{ path('app_site_page', { path: 'formats' }) }}" class="transition hover:text-white">Форматы</a></li>
                    <li><a href="{{ path('app_site_page', { path: 'about' }) }}" class="transition hover:text-white">О компании</a></li>
                    <li><a href="{{ path('app_homepage') }}#faq" class="transition hover:text-white">FAQ</a></li>
                </ul>
            </nav>

            <div>
                <h5 class="mb-4 text-[.78rem] font-bold uppercase tracking-[0.08em] text-white">Контакты</h5>
                <ul class="space-y-3 text-[.95rem]">
                    <li><a href="tel:+79834366777" class="transition hover:text-white">8 (983) 436-67-77</a></li>
                    <li><a href="mailto:rk@domline.ru" class="transition hover:text-white">rk@domline.ru</a></li>
                    <li class="max-w-[18rem]">г. Улан-Удэ, ул. Смолина, 54а</li>
                    <li>Пн–Пт, 9:00–18:00</li>
                </ul>
            </div>
        </div>

        <div class="mt-10 flex flex-wrap items-center justify-between gap-3 border-t border-white/10 pt-6 text-[.86rem]">
            <span>© {{ 'now'|date('Y') }} Рекламные конструкции 03. Все права защищены.</span>
            <a href="{{ path('app_site_page', { path: 'contact' }) }}" class="transition hover:text-white">Связаться с нами →</a>
        </div>
    </div>
</footer>
